# start a new project
``` shell
uv init
```

# install dependencies
``` shell
uv add jupyter matplotlib numpy torch
```

# run notebooks
``` shell
uv run jupyter lab
```
`names.txt` can be found in the `makemore` repo: https://github.com/karpathy/makemore
